#include "main.h"

/**
 * _abs - print the absolute value of an integer
 *
 * @a: input num
 *
 * Return: the absolute value of the input
 */

int _abs(int a)
{
	if (a < 0)
	{
		return (-a);
	}
	return (a);
}
